import { GlobalValidationDirective } from './global-validation.directive';

describe('GlobalValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalValidationDirective();
    expect(directive).toBeTruthy();
  });
});
